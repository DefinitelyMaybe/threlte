---
import { Code as SLCode } from '@astrojs/starlight/components'

type MarkerDefinition = string | RegExp | number | { range: string; label?: string | undefined }

interface Props {
  code: string
  class?: string
  title?: string
  lang?: string
  frame?: 'auto' | 'code' | 'terminal' | 'none' | undefined
  mark?: MarkerDefinition | MarkerDefinition[]
  ins?: MarkerDefinition | MarkerDefinition[]
  del?: MarkerDefinition | MarkerDefinition[]
  wrap?: boolean
}
---

<SLCode {...Astro.props} />
